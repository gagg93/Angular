import { MyTableActionEnum } from './my-table-action-enum';

describe('MyTableActionEnum', () => {
  it('should create an instance', () => {
    expect(new MyTableActionEnum()).toBeTruthy();
  });
});
